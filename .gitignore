# Real config with credentials — never commit this
media_agent_config.json

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Output indexes — generated data, not source
movies.json
movies_tmdb.json
movies_below_720p.csv
tvshows.json
music.json

# ffmpeg — downloaded by scripts/install_ffmpeg.*, not committed (GPL)
vendor/ffmpeg/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Review handoffs and result docs -- internal, not part of the published
# project. Both directions (brief -> reviewer, findings -> us) live here.
codereview/
